---
name: gis-feature-initiative
description: Ongoing effort (started ~Sept 2026) to add GIS/geospatial features
  to json-data-manager, benchmarked against GeoLens; first batch (#24–#32)
  completed 2026-09-15 via PRs #33–#37
metadata:
  node_type: memory
  type: project
  originSessionId: sess_00663030-8290-4470-a02a-37c4c2819d2a
---

As of 2026-09-15, the user is adding GIS-related features to json-data-manager to bring it up to par with geospatial web apps like GeoLens. The first batch (issues #24–#32) is **complete**: merged as PRs #33 (type filter + tags), #34 (conversion completion UX + #31 regression tests), #35 (individual points, map above table, extent bbox), #36 (collection extent map), #37 (collapsible JSON definition). All 9 issues closed. Follow-ups the same day: **PR #38 — group pages** at `/collections/$collectionId/groups/$groupId` with a combined feature-layer map (per-dataset colors, legend, feature popups via `listEntriesForSchemas`) above the member dataset list; `GroupMap`, `DatasetList`, and `SchemaGeometriesLoader`/`useGeometriesBySchemas` are shared components extracted for reuse. **PR #39 — extent-fit framing**: the `Map` component's `bounds` is now reactive (re-fits when bounds values change, e.g. when externally-stored geometries resolve late) with a default 40px `fitBoundsOptions` inset, and the group map draws the same dashed extent outline as dataset maps — datasets, groups, and collections all load zoomed to their full extent. **PR #40 — export dialog**: datasets and groups export via a format-picker dialog (`ExportDialog` + `app/src/lib/export.ts`); geospatial datasets get GeoJSON (default)/JSON/Excel, regular get JSON/Excel; group GeoJSON/JSON = one file per dataset (never mixed schemas), group Excel = one multi-sheet workbook; include-schema checkbox adds per-dataset `-schema.json` downloads. Excel writing uses `exceljs` (dynamic import, already in tree for xlsx import parsing). **PR #41 — the include-schema checkbox defaults to unchecked** (users opt in to the schema file). **PR #42 — dataset organization breadcrumb**: when a dataset is in a group, the group name in the folder-icon badge line links to the group page (previously plain text).

**Key facts settled during the batch:** #31 (lat/long "erasure") was **not reproducible** — live tiger-dataset records keep Latitude/Longitude and the conversion primitives never touch `entry.data`; closed with regression tests in `packages/json-cms/src/component/lib.test.ts` guarding property preservation. `MapClusterLayer` still supports clustering behind its `cluster` prop (default off) for a future display option. `bboxFeature` lives in `app/src/lib/point-geometry.ts`; `CollectionExtentMap` replaced the old `DatasetsMap`. The group filter on the collection map was scoped out of #30. The denormalized `boundingBox` on schema docs is non-shrinking after deletions; since `31b053e` the dataset map opens on it immediately and only re-fits to client-computed `computeBbox` once loading completes (the earlier "computeBbox preferred" stance applied to the initial viewport, which must not wait on data).

**Local dev data (as of 2026-09-15, changes freely):** the tiger dataset `j97660chj93zdj8tehke412and8ecwhc` is in NO collection/group, while a similarly-named "TIGER / BUILD / RAISE Awards (2009-2026)" (`j972x6rryq1hpv9k87ng7n1mdx8ec46g`) IS in the TIGER group — don't confuse them when verifying org-breadcrumb/group features (user may re-assign TIGER via Organize dialog). Good multi-dataset verification fixture: "SS4A Awards" group `jn762nc8mvt85gb6s2pws2nhqd8edmd7` in the "Federal Transportation Funding" collection `js7dpykbqp18fjb9z8zv5b8ncn8eds4j` — 3 datasets (point/line/polygon) spanning HI→PR→WI.

**Local DB snapshot (created 2026-09-15, at user request, NOT committed):** `exports/convex-local-snapshot-2026-09-15.zip` (121 MB) sits untracked in the working tree and is **not gitignored** — an accidental `git add .` would stage it; the user said "don't commit yet" so it may be intentionally kept or gitignored later. Made with `bunx convex export --path <zip> --include-file-storage` run from `app/` (flag is `--include-file-storage`, not `--include-storage`; running it without args from the root just prints help). Contains the full local-deployment snapshot: all jsonCms tables (36 datasets / 1385-entry tiger / 222 MB uncompressed geometries), file-storage blobs, and workflow internals — including the 3 throwaway "Conversion UX Test" datasets. Restore with `bunx convex import --path <zip>` (add `--replace` only to overwrite an existing deployment).

**Organization model rework (PR #43, MERGED 2026-09-15 as ae4ea47) — SUPERSEDES the org facts above:** collections are now **many-to-many** (new `schemaCollections` join table in the jsonCms component; `setSchemaCollection` → `addSchemaToCollection`/`removeSchemaFromCollection`/`listCollectionsBySchema`/`listSchemaCollections`), and **groups are standalone-capable** (`groups.collectionId` optional). Group and collection membership are independent (group assignment no longer implies/derives collection membership). The group page moved from `/collections/$collectionId/groups/$groupId` to **`/groups/$groupId`**. The dataset page gained a default **Overview tab** (details card + inline collection/group add/edit/remove) replacing the organize sheet; tabs are now Overview | Data | Schema (Entries renamed). The `/datasets` browser is hierarchical: groups list member datasets beneath them and grouped datasets don't appear at top level. **The dev deployment's data was wiped** for this migration (user-approved); the funding-sources datasets need re-importing via Bulk Upload — the pre-reset data survives only in the `exports/` snapshot zip below, so all dataset/group/collection ids mentioned above are dead on the live deployment.

**Post-#43 UX pass (PR #44, MERGED 2026-09-15 as merge commit 218690c; branch `feature/entry-details-and-map-ux` deleted):** fifteen commits — `991f7c1` DatasetInfoCard (title/description card above the editor panels in `packages/json-cms` SchemaEditor, replacing the Visual-builder's fields), `56a3a27` dataset map moved above the tab bar + skeleton until every geometry resolves, `cdee16d` dataset description now optional (title only required), `28aca93` Data→Visual tab switch works directly (was gated on coming from Code), `5472fe4` DatasetInfoCard fields stacked vertically, `bb04506`+`c3df0e2` group member rows show type tags stacked above the title, `30b08ef` dataset detail breadcrumb includes the group (Datasets / {group} / {dataset}, linking to `/groups/$groupId`; crumb skipped for dangling groupIds), `5c712c3` detail tabs driven by `?view=` search param (default "overview" omitted; panel navigations merge params), `edebaeb` Schema tab renamed "Structure" (`?view=structure`), `229150b` map skeleton waits for a FULL pagination pass (`useAllPaginated` now exposes `status`; gate on `status === "Exhausted"` + all rows resolved, latch after first complete render), `83cd756` `DatasetTypeTags` extracted into shared `app/src/components/dataset-type-tags.tsx` and rendered on every `DatasetList` row, `e0f082d` DatasetList rows show counts (geospatial: `featureCount` → "N features"; regular: "N rows" from an optional `entryCounts` map the group page derives from already-fetched entries), and `fe7d969` **entry details page rebuilt** (`/datasets/$schemaId/$entryId`): key-value properties table (~2/3 width, raw-JSON fallback for non-object data), geospatial rail (~1/3) with a minimap (geometry + dashed bbox, dataset-map styling) over a collapsible raw-GeoJSON viewer, and a header entry-size readout (properties + geometry as one JSON object); fed by a new `getEntryGeometry` component query (indexed `by_entry` read) exposed through `exposeApi` + `app/convex/geometries.ts` (chain in [[local-dev-verification]]); dataset and group map feature panels gained "View details" links to the page; `formatPropertyValue`/`formatBytes` moved to `app/src/lib/format.ts`; `MetadataEditForm` accepts the optional description. Workflow note: since the commits sat on local main, salvaging was just `git checkout -b feature/entry-details-and-map-ux` + push + `gh pr merge --merge --delete-branch` (no CI checks configured) — gh checked main out and fast-forwarded it through the merge commit; no reset needed. The user re-imported data post-wipe: SS4A FY22 grant datasets (one MultiPolygon geospatial, others in a standalone "SS4A FY22 Implementation Grant Awards" group) were visible on the live local deployment. See [[ui-polish-expectations]] for the UX principles behind them.

**Maps feature (PR #47, MERGED 2026-09-16 as d37720d (`gh pr merge --merge --delete-branch`; 7 commits incl. a last-minute collection-add-sheet lint refactor; branch deleted local+remote): [bf1b5bd maps views, 0d39ba0 add-to-map entry points, 5cb1d76 groups-as-units, f506e06 nested layer groups, 1411a1a fit-once viewport, b97cd41 child-eye toggle fix]** GeoLens-style saved map views as a **top-level nav item next to Collections**. Component side: `maps` + `mapLayers` tables (layer = `{mapId, order, targetType, targetId: union-of-typed-ids, visible}`; `by_map` index is `[mapId, order]` so an index scan IS draw order) + **`mapLayerOverrides`** (commit 4: per-child visibility rows `{layerId, childKey: "group:<id>"|"dataset:<id>", visible}`, only explicitly-toggled children get rows, stale keys inert, cascaded via `deleteMapLayersForLayer` which removeMapLayer/deleteMap/delete-target-cascades all route through); mutations exposed via `exposeApi` (auth-op union gained `mapId`) + `app/convex/maps.ts`: map CRUD, `addMapLayer` (dup no-op), `moveMapLayer`, `setMapLayerVisibility`, `setMapLayerOverride` (hide inserts {visible:false}, show/undefined clears — commit 6 b97cd41 fixed the UI handler that had this ternary inverted, making the child eye a silent no-op), `listMapLayerOverrides`; **deleting a collection (incl. nested groups)/group/dataset cascades to its map layers**. **Commit 2 — group-as-dataset-unit**: `setGroupCollection` mutation (mirrors `setSchemaGroup`; a group lives in ≤1 collection, adding moves) + hierarchical `CollectionAddSheet` (browser-style: groups as units w/ members nested, search shows all members when group name matches) replacing the flat picker on the collection page (group-target adds keep DatasetPickerSheet); "group treated as a single dataset" flows into: collection page extent map + `collectionContentDatasets` derivation, /collections card counts, Maps layer-picker collection counts, and `expandLayerDatasets` (collection expansion = direct memberships + member-group datasets). **Commit 4 — nested layer groups**: `MapLayerPanel` renders an expandable tree (collection → groups → datasets, group → datasets) with per-child eye toggles writing overrides; `resolveVisibleSchemaIds` unions visible layers minus overrides plus hidden-group cascade (hidden group child hides its datasets regardless of their own overrides); expansion is an inverted collapsed-set so new layers default expanded. Add-to-map entry points (commit 2 of the maps feature): `AddToMapSheet` on collection/group headers + dataset More-actions dropdown (geospatial only). Gotchas: repo oxlint **bans optional chaining**, rejects `useMemo` with `x && y ? … : undefined`, complexity ≤10 per function (page components 21–30 exist as tolerated warnings — extract hooks/helpers when a NEW file trips it), `react/no-array-index-key`, `unicorn/consistent-function-scoping`; `MapIterator` has no `.flat()`; zsh doesn't word-split `$VAR`; `convex run` uses `.env.local` defaults; **restart the app dev session after component dist rebuilds** (two rebuild+restart cycles this session; dev lands on :3001/:3002 when 3000 is taken). E2E verified via `convex run` from `app/` (all targetTypes, overrides hide/clear/cascade). **Commit 5 — fit-once viewport**: LayersMap captures bounds ONCE at mount from the rows' own per-row `bbox` envelopes (lazy useState initializer — no payload resolution needed), so toggles/overrides/adds never move the camera; the loading gate serves `servedGeometries` (new field on `useGeometriesBySchemas`: loaded-so-far rows while a new schema streams in) and latches `hasLoadedOnce` via a `CompletionLatch` child component — direct setState-in-effect trips the React Compiler `react(set-state-in-effect)` rule (entries-map carries the identical pattern as a PRE-EXISTING baseline error), and refs during render trip `react(refs)` (both banned; the child-callback pattern passes). "Nothing to draw" is now an overlay on the mounted Map, not a replacement. Repo lint baseline: 16 errors on main — entries-map's own set-state-in-effect latch is one of them; new code must stay at 0 new errors.

**Why:** The initiative is the driving goal for current work; issue/PR numbers and the #31 finding are external state not re-derivable from code alone (the tests encode it, but the "was reported by the user, investigated live" context isn't).

**How to apply:** When proposing the next GIS batch, favor GeoLens-comparable capabilities (the user shares GeoLens screenshots as UX reference — dataset page with vector/geometry badges, extent maps, collection stats). Dev environment: Bun workspace monorepo; `bun run dev` at repo root starts Convex local backend (127.0.0.1:3212) plus the TanStack Start/Vite app on http://localhost:3000. Component dev gotchas: `packages/json-cms` needs `bun run build:codegen` after component changes — its `.env.local` deployment was fixed to `local:local-caden_young_noblis_org-json_cms_dev` and its local backend moved to ports **3216/3217** (3210 is another project, uls-search; don't kill it). Convex dev sessions don't reliably re-push on workspace-component dist changes — if the deployment is stale, restart the app dev session (three concurrent `convex dev` sessions were found fighting on 2026-09-15). Never `git stash` to compare against main while the vite watcher is live: it regenerates `routeTree.gen.ts` and silently blocks `git stash pop`. Repo-wide `bun run fmt` reformats ~16 files per run (oxfmt drift) — format only touched files; oxlint rules that bite new code: optional chaining BANNED, `useMemo` with `x && y ? … : undefined` rejected by the React-Compiler memo lint, complexity ≤10 per function, `react/no-array-index-key`, `unicorn/consistent-function-scoping`, and most importantly — since 2026-09-16 — **`react(set-state-in-effect)` and `react(refs)` both reject latch patterns**: setState inside an effect or touching `ref.current` during render are compile errors; the sanctioned latch is a child component whose effect calls a parent callback prop (the `SchemaGeometriesLoader`/`CompletionLatch` pattern), and lazy `useState` initializers for mount-time captures. Main carries 16 pre-existing lint errors (incl. entries-map's set-state-in-effect latch as one) and ~70 tolerated warnings (page-complexity 21–30 on big route components is normal); new code must add ZERO errors and should add zero warnings to NEW files. Base UI Collapsible trigger exposes `data-panel-open` (not `data-open`) and the WebKit-based in-app browser misreports computed `rotate` — use inline style or verify visually. **`main` rejects direct `git push`** (branch protection) — after merging a PR, `git checkout main` leaves you on main; branch off BEFORE committing (a stray commit on main can be salvaged via `git branch <name> <sha>` + `git reset --hard origin/main`).
