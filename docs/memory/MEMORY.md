# Memory Index

> Project memory is versioned with the repo. See the memory policy in
> `AGENTS.md` / `CLAUDE.md`. Full audit context: `../map-performance-audit-2026-09-16.md`.

- [GeoLibre research](geolibre-research.md) — opengeos/GeoLibre (MIT): size-budgeted snapshot undo (500k feature proxy, reference dedup, data:-URL charging, 400ms coalesce), DuckDB-WASM Spatial local engine + deck.gl shared overlay, RFC 7946 six-value bbox guard
- [Geometry transport research](geometry-transport-research.md) — Placemark (MIT) + GeoLens (Apache-2.0) patterns for geometry loading/change capture; validates #51 version-bump design (GeoLens tile_cache_version) and per-feature inverse-patch undo (Placemark Moments); file refs inside; note: subagent spawning unavailable on this plan ("no reasoning level selected" on any Agent launch) — research runs in main thread
- [Map performance audit 2026-09](map-performance-audit-2026-09.md) — 2026-09-16 Convex perf audit; "maps slow to load points" root causes: 8-row page cap (57 serial pages / 46.55 MB for FY22 dataset), serial effect-gated loadMore loop, collection extent map loading all geometries instead of using schemas.boundingBox; issues #48–#55 (performance label); 6dp simplification NOT a perf lever (source already ≤6dp); #51 blob design refined: invalidate-on-write + lazy debounced rebuild (never reassemble per geometry edit), imports rebuild at handleImportComplete, blob path only above a size threshold

- [GIS feature initiative](gis-feature-initiative.md) — adding geospatial features to match GeoLens; PRs #33–#42 (batch #24–#32 + follow-ups) done 2026-09-15; **PR #43 org rework**: many-to-many collections (schemaCollections join table), standalone groups, Overview tab replaced organize sheet, datasets browser hierarchical, dev data WIPED (user has since re-imported SS4A data; pre-reset data only in exports/ snapshot); **PR #44 merged 2026-09-15** (fifteen UX commits: Dataset info card, optional description, map above tabs with full-pass skeleton gate + stored-extent instant open, type tags + row/feature counts on group/collection lists, group breadcrumbs, `?view=` URL tabs with Structure rename, entry details rebuilt — properties table 2/3 + minimap/GeoJSON rail 1/3 + entry size, new `getEntryGeometry` query); **PR #47 MERGED 2026-09-16 as d37720d**, branch `feature/map-views` deleted (7 commits incl. child-eye toggle fix b97cd41): Maps top-level nav + saved map views — maps/mapLayers + mapLayerOverrides component tables, layer CRUD + delete-target cascades, /maps + /maps/$mapId workspace, live layer expansion, geometry-only rendering, instant show/hide, `setGroupCollection` (groups join collections as single units — CollectionAddSheet mirrors the datasets browser), nested layer-group visibility (collection → groups → datasets, per-child eye toggles), and fit-once viewport (bounds latched from row bboxes at mount; servedGeometries keeps the Map mounted); only `main` remains locally — always `git fetch --prune` after merges and branch off fresh from main next; oxlint bans optional chaining/setState-in-effect/render-phase refs; component dist rebuilds need an app-dev-session restart; json-cms component backend on ports 3216/3217, app via `bun run dev`
- [Geometry precision experiment](geometry-precision-experiment.md) — issue #45 analysis → PR #46 merged: simplify-geometry checkbox (default on) + source-file retention + on-demand simplification workflow; 6dp duplicate dataset `jx78knwxh0aqmnj2jgfg80st318efn5j` still in dev, deletable
- [Local dev & verification](local-dev-verification.md) — dev stack often already running (lsof shows :3000 as `hbci`); in-app browser can't reach localhost, verify via curl SSR; Convex pages SSR as spinner only; blank page = check for TanStack Router generator stub clobbering a route file (`git restore` fixes); app consumes `@caden/json-cms` from dist — `bun run build` in `packages/json-cms` after package edits, verify via `/@fs/` module fetch; app has no typecheck script (`bunx tsc --noEmit`); NO local Convex dashboard on 6790 — `convex dev` tunnels the local deployment to the cloud dashboard (URL printed in the dev log; backend serves only 3212 + internal 3213)
- [Worktrees & port-isolated dev](dev-worktrees-ports.md) — `bun run worktree add|clean`, `scripts/dev.ts` orchestrator with per-checkout vite ports + shared/forked `.convex`, attach mode; **stale root `.env.local` (cloud `dev:different-elk-353`) poisons bun-spawned convex** — orchestrator scrubs `CONVEX_DEPLOYMENT` + pins `--env-file`; vite needs `--host 127.0.0.1`; post-merge hook needs `bun run setup` per clone
- [UI polish expectations](ui-polish-expectations.md) — surface required fields prominently (never a silently disabled Save or no-op control), skeleton maps until ALL data loaded — completeness = pagination `status === "Exhausted"` + all rows resolved (NOT `isLoading`, which drops after the first page), latch after first complete render so live updates never re-skeleton (commit 229150b); with a server-maintained extent (`schemas.boundingBox`) skip the skeleton — instant map on the stored extent + corner "Loading features…" spinner, one exact refit at completion (31b053e); dataset map above the tab bar; description optional — title is the only required field (3 enforcement layers change together: component lib.ts, component schema.ts, schemaMetaError); tab handlers must work from every tab (Data→Visual); group member rows show the same type tags as standalone cards, stacked above the title (replace redundant icons); view state in URL (`?view=entries|structure`, default omitted) — navigate search params via merge form, never replace; "X or find a better way" = pick the better design yourself
