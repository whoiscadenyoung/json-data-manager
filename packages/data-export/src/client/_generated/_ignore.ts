// This is only here so convex-test can detect a _generated folder. The
// content is irrelevant — a named export keeps linters happy without
// pretending to be a real module.
export const GENERATED_FOLDER_MARKER = true;
