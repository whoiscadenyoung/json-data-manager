"use client";

// Placeholder for React helpers/hooks. The export component is backend-first;
// consumers typically drive it with the generated Convex React hooks against
// the functions produced by `exposeApi`. Add shared UI/hooks here as needed.

